<!DOCTYPE html>
<html lang="vi">
    <head>
        <meta charset="UTF-8" />
        <meta http-equiv="X-UA-Compatible" content="IE=edge" />
        <meta name="viewport" content="width=device-width, initial-scale=1.0" />
        <title><%= (acc && acc.title) || 'Chi tiết lưu trú' %> | HÀ NỘI</title>
        <meta name="description" content="<%= (acc && (acc.title + ' - Lưu trú Hà Nội')) || 'Chi tiết lưu trú Hà Nội' %>" />
        <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/4.7.0/css/font-awesome.min.css" />
        <link rel="stylesheet" href="/client/css/style.css" />
        <link rel="stylesheet" href="/client/css/responsive.css" />
        <link rel="stylesheet" href="/client/css/harmony.css" />
        <link rel="stylesheet" href="/client/css/accommodation-detail.css" />
        <link rel="stylesheet" href="https://unpkg.com/maplibre-gl@3.6.1/dist/maplibre-gl.css" />
        <link rel="apple-touch-icon" sizes="180x180" href="/client/img/apple-touch-icon.png" />
        <link rel="icon" type="image/png" sizes="32x32" href="/client/img/favicon-32x32.png" />
        <link rel="icon" type="image/png" sizes="16x16" href="/client/img/favicon-16x16.png" />
        <link rel="manifest" href="/client/img/site.webmanifest" />
    </head>
    <body class="accommodation-detail-page">
        <%- include('../../partials/header') %>

        <!-- Banner -->
        <header class="page-header accommodation-banner" style="background-image: url('<%= (acc && (acc.heroImage || (acc.images && acc.images[0]))) || '/client/img/header-bg.jfif' %>');">
            <div class="header-content">
                <h2 id="quote">Lưu trú & Nghỉ dưỡng</h2>
                <div class="line"></div>
                <h1><%= (acc && acc.title) || 'Lưu trú' %></h1>
                <p class="font-color">Trải nghiệm nghỉ dưỡng tuyệt vời tại Hà Nội</p>
            </div>
        </header>

        <main class="acc-detail main">
            <div class="container main-content">
                <!-- Gallery -->
                <section class="image-gallery" id="gallery">
                    <% var imgs = (acc && acc.images && acc.images.length) ? acc.images : ['/client/img/carousel-img2.jpg','/client/img/carousel-img3.jpg','/client/img/img1.jfif','/client/img/img2.jfif','/client/img/img3.png']; %>
                    <% imgs.slice(0, 5).forEach(function(img, idx){ %>
                    <div class="gallery-item" data-index="<%= idx %>">
                        <img src="<%= img %>" alt="Hình ảnh lưu trú" loading="lazy" />
                        <% if (idx === 0) { %>
                        <button class="show-all-photos" type="button">
                            <i class="fa fa-images"></i> Xem tất cả ảnh
                        </button>
                        <% } %>
                    </div>
                    <% }) %>
                </section>

                
                <div class="content-grid">
                    <div>
                        <!-- Intro -->
                        <section class="main-section">
                            <h2 class="section-title">Giới thiệu</h2>
                            <div class="description">
                                <% var intro = (acc && acc.intro && acc.intro.length) ? acc.intro : []; %>
                                <% if (!intro.length) { %>
                                    <p>Thông tin đang được cập nhật.</p>
                                <% } else { intro.forEach(function(p){ %>
                                    <p><%= p %></p>
                                <% }) } %>
                            </div>
                            <% if (acc && acc.highlights && acc.highlights.length) { %>
                            <div class="highlight-box">
                                <h3><i class="fa fa-award"></i> Điểm nổi bật</h3>
                                <ul>
                                    <% acc.highlights.forEach(function(h){ %>
                                    <li><%= h %></li>
                                    <% }) %>
                                </ul>
                            </div>
                            <% } %>

                            <% if (acc && acc.roomFeatures && acc.roomFeatures.length) { %>
                            <div class="room-info">
                                <h3><i class="fa fa-bed"></i> Tiện nghi trong phòng</h3>
                                <div class="room-features-list">
                                    <% acc.roomFeatures.forEach(function(f){ %>
                                    <div class="feature-item"><i class="fa fa-check-circle"></i><span><%= f %></span></div>
                                    <% }) %>
                                </div>
                            </div>
                            <% } %>
                        </section>

                        <!-- Services -->
                        <% var services = (acc && acc.services) || {}; %>
                        <section class="services-section">
                            <h2 class="section-title">Dịch vụ & Tiện ích</h2>
                            <% if (!services.common && !services.service && !services.breakfast) { %>
                                <p class="font-color">Thông tin đang được cập nhật.</p>
                            <% } %>
                            <% if (services.common && services.common.length) { %>
                            <div class="service-category">
                                <h3>Tiện nghi chung</h3>
                                <div class="service-list">
                                    <% services.common.forEach(function(s){ %>
                                    <div class="service-item"><i class="fa fa-check"></i><span><%= s %></span></div>
                                    <% }) %>
                                </div>
                            </div>
                            <% } %>
                            <% if (services.service && services.service.length) { %>
                            <div class="service-category">
                                <h3>Dịch vụ</h3>
                                <div class="service-list">
                                    <% services.service.forEach(function(s){ %>
                                    <div class="service-item"><i class="fa fa-check"></i><span><%= s %></span></div>
                                    <% }) %>
                                </div>
                            </div>
                            <% } %>
                            <% if (services.breakfast && services.breakfast.length) { %>
                            <div class="service-category">
                                <h3>Bữa sáng</h3>
                                <div class="service-list">
                                    <% services.breakfast.forEach(function(s){ %>
                                    <div class="service-item"><i class="fa fa-check"></i><span><%= s %></span></div>
                                    <% }) %>
                                </div>
                            </div>
                            <% } %>
                        </section>

                        <!-- Nearby places -->
                        <% var near = (acc && acc.nearbyPlaces) || []; %>
                        <% if (near.length) { %>
                        <section class="nearby-places">
                            <h2 class="section-title">Địa điểm gần đây</h2>
                            <% near.forEach(function(p){ %>
                            <div class="place-item">
                                <div class="place-name">
                                    <i class="fa <%= p.icon || 'fa-map-marker' %>"></i>
                                    <span><%= p.name %></span>
                                </div>
                                <div class="place-distance"><%= p.distance %></div>
                            </div>
                            <% }) %>
                        </section>
                        <% } %>

                        <!-- Map moved below to span full width -->
                    </div>

                    <aside class="sidebar">
                        <!-- Booking card (placeholder form) -->
                        <section class="booking-card">
                            <h3><i class="fa fa-calendar-check-o"></i> Đặt phòng</h3>
                            <form>
                                <div class="form-group">
                                    <label><i class="fa fa-calendar"></i> Ngày nhận phòng</label>
                                    <input type="date" required />
                                </div>
                                <div class="form-group">
                                    <label><i class="fa fa-calendar"></i> Ngày trả phòng</label>
                                    <input type="date" required />
                                </div>
                                <div class="form-group">
                                    <label><i class="fa fa-users"></i> Số khách</label>
                                    <select>
                                        <option>1 người</option>
                                        <option>2 người</option>
                                        <option>3 người</option>
                                        <option>4 người</option>
                                    </select>
                                </div>
                                <div class="form-group">
                                    <label><i class="fa fa-door-open"></i> Loại phòng</label>
                                    <select>
                                        <option>Studio Standard</option>
                                        <option>Studio Deluxe</option>
                                        <option>Studio Premium</option>
                                    </select>
                                </div>
                                <button type="submit" class="btn-primary">
                                    <i class="fa fa-paper-plane"></i> Đặt phòng ngay
                                </button>
                            </form>
                            <div class="benefits-box">
                                <div class="benefit-item"><i class="fa fa-check-circle"></i><span>Đặt phòng an toàn</span></div>
                                <div class="benefit-item"><i class="fa fa-check-circle"></i><span>Xác nhận tức thì</span></div>
                                <div class="benefit-item"><i class="fa fa-check-circle"></i><span>Hỗ trợ 24/7</span></div>
                            </div>
                        </section>

                        <!-- Contact info -->
                        <% var contacts = (acc && acc.contacts) || {}; %>
                        <section class="contact-info">
                            <h3><i class="fa fa-address-book"></i> Thông tin liên hệ</h3>
                            <% if (contacts.phone) { %>
                            <div class="contact-item">
                                <i class="fa fa-phone"></i>
                                <div>
                                    <!-- <div class="label">Điện thoại</div> -->
                                    <div class="value"><%= contacts.phone %></div>
                                </div>
                            </div>
                            <% } %>
                            <% if (contacts.email) { %>
                            <div class="contact-item">
                                <i class="fa fa-envelope"></i>
                                <div>
                                    <!-- <div class="label">Email</div> -->
                                    <div class="value"><%= contacts.email %></div>
                                </div>
                            </div>
                            <% } %>
                            <% if (contacts.address || (acc && acc.address)) { %>
                            <div class="contact-item">
                                <i class="fa fa-map-marker"></i>
                                <div>
                                    <!-- <div class="label">Địa chỉ</div> -->
                                    <div class="value"><%= contacts.address || (acc && acc.address) || '' %></div>
                                </div>
                            </div>
                            <% } %>
                        </section>
                    </aside>
                </div>

                <!-- Map (full-width) -->
                <section class="map-container">
                    <h2 class="section-title">Vị trí trên bản đồ</h2>
                    <div id="accMap"
                         class="acc-map-embed"
                         data-lat="<%= (acc && acc.map && acc.map.lat) || 21.028511 %>"
                         data-lng="<%= (acc && acc.map && acc.map.lng) || 105.804817 %>"
                         data-zoom="<%= (acc && acc.map && acc.map.zoom) || 14 %>"
                         data-name="<%= (acc && acc.title) || '' %>"
                         data-address="<%= (acc && acc.address) || '' %>"
                         data-image="<%= (acc && (acc.heroImage || (acc.images && acc.images[0]))) || '' %>"
                         data-url="<%= (acc && ('/accommodation/' + (acc.slug || ''))) || '' %>"></div>
                    <% if (acc && acc.address) { %>
                    <p class="map-address"><strong>Địa chỉ:</strong> <%= acc.address %></p>
                    <% } %>
                </section>
            </div>
        </main>

        <!-- up scroll -->
        <i class="arrow" onclick="topFunction()" id="upbtn"></i>

        <!-- Reviews Section (similar to attraction) - at bottom -->
        <section id="reviews" class="content-section reviews-pro">
            <div class="reviews-pro__header">
                <div class="reviews-pro__heading">
                    <h2>Đừng chỉ nghe chúng tôi, hãy nghe du khách</h2>
                    <p class="subheading">Chúng tôi yêu du khách của mình – và rất vui vì điều đó là tương hỗ.</p>
                </div>
                <div class="reviews-pro__provider">
                    <img class="reviews-pro__provider-icon" src="/client/img/google.svg" alt="Google reviews" />
                    <div class="reviews-pro__provider-stats">
                        <span class="overall"><%= (acc && acc.rating) || '4.8' %></span>
                        <span class="stars" aria-label="5 sao">
                            <i class="fa fa-star"></i><i class="fa fa-star"></i><i class="fa fa-star"></i><i class="fa fa-star"></i><i class="fa fa-star"></i>
                        </span>
                        <a class="reviews-pro__link" href="#" target="_blank" rel="noreferrer">Xem tất cả đánh giá</a>
                        <span class="total">(<%= (acc && acc.reviewCount) || '269' %>)</span>
                    </div>
                </div>
            </div>

            <div class="reviews-pro__slider">
                <button class="reviews-pro__nav prev" type="button" aria-label="Trước">&#10094;</button>
                <div class="reviews-pro__viewport">
                    <div class="reviews-pro__track">
                        <% var rv = (acc && acc.reviews && acc.reviews.length) ? acc.reviews : [
                            { name: 'Linh', country: 'Việt Nam', rating: 5, text: 'Phòng sạch, spa tuyệt vời.' },
                            { name: 'An', country: 'Việt Nam', rating: 5, text: 'Vị trí tiện đi lại, nhân viên thân thiện.' },
                            { name: 'Jade', country: 'Singapore', rating: 4, text: 'Rất ổn so với tầm giá.' },
                            { name: 'Minh', country: 'Việt Nam', rating: 5, text: 'Không gian yên tĩnh, decor đẹp.' }
                        ]; rv.forEach(function(r){ %>
                        <div class="reviews-pro__card">
                            <img class="reviews-pro__quote" src="/client/img/quote.svg" alt="Quote" />
                            <div class="reviews-pro__text"><%= r.text %></div>
                            <div class="reviews-pro__stars">
                                <% for (var i=0;i<r.rating;i++){ %><i class="fa fa-star"></i><% } %>
                            </div>
                            <div class="reviews-pro__meta">
                                <span class="name"><%= r.name %></span>
                                <span class="dot"></span>
                                <span class="country"><%= r.country %></span>
                            </div>
                        </div>
                        <% }); %>
                    </div>
                </div>
                <div class="reviews-pro__dots" role="tablist" aria-label="Chuyển trang đánh giá"></div>
                <button class="reviews-pro__nav next" type="button" aria-label="Sau">&#10095;</button>
            </div>
        </section>

        <!--footer-->
        <%- include('../../partials/footer') %>
        <!--footer-->

        <script src="https://code.jquery.com/jquery-3.6.0.js"></script>
        <script src="/client/js/includes.js"></script>
        <script src="/client/js/script.js"></script>
        <script src="https://unpkg.com/maplibre-gl@3.6.1/dist/maplibre-gl.js"></script>
        <script src="/client/js/accommodation-detail.js"></script>
    </body>
    </html>


