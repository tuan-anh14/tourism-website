<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>Chức năng đang phát triển</title>
  <style>
    /* Reset some basic elements */
    * {
      box-sizing: border-box;
    }
    body, html {
      margin: 0;
      padding: 0;
      height: 100%;
      font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, Oxygen,
        Ubuntu, Cantarell, "Open Sans", "Helvetica Neue", sans-serif;
      background-color: #f0f2f5;
      display: flex;
      align-items: center;
      justify-content: center;
      color: rgba(0, 0, 0, 0.65);
    }
    .container {
      text-align: center;
      padding: 24px;
      background: #fff;
      border-radius: 8px;
      box-shadow:
        0 2px 8px rgba(0, 0, 0, 0.15);
      max-width: 400px;
      width: 90%;
    }
    .status {
      font-size: 96px;
      font-weight: 700;
      color: rgba(0, 0, 0, 0.85);
      margin: 0;
      line-height: 1;
    }
    .title {
      font-size: 24px;
      font-weight: 600;
      margin: 16px 0 8px;
    }
    .subtitle {
      font-size: 14px;
      color: rgba(0, 0, 0, 0.45);
      margin-bottom: 24px;
    }
    .btn-primary {
      background-color: #1890ff;
      color: #fff;
      border: none;
      padding: 8px 24px;
      font-size: 16px;
      border-radius: 4px;
      cursor: pointer;
      transition: background-color 0.3s ease;
      text-decoration: none;
      display: inline-block;
    }
    .btn-primary:hover {
      background-color: #40a9ff;
    }
  </style>
</head>
<body>
  <div class="container" role="main" aria-labelledby="status-title" aria-describedby="status-desc">
    <h1 class="status" id="status-title">404</h1>
    <div class="title">Rất tiếc, trang bạn truy cập không tồn tại.</div>
    <a href="/" class="btn-primary" role="button">Quay về trang chủ</a>
  </div>
</body>
</html>
